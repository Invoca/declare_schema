
---
apiVersion: v1
kind: Pod
metadata:
  labels:
    jenkins/declare_schema: 'true'
  namespace: jenkins
  name: declare_schema
spec:
  containers:
  - name: ruby
    image: ruby:2.6.5
    tty: true
    resources:
      requests:
        memory: "100Mi"
    command:
      - cat
